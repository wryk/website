{
  "license": "UNLICENSED",
  "source": "src/index.pug",
  "scripts": {
    "build": "parcel build",
    "deploy": "netlify deploy --prod --build -d dist",
    "dev": "parcel"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "^2.2.0",
    "autoprefixer": "^10.4.2",
    "jstransformer-markdown": "^1.2.1",
    "netlify-cli": "^8.8.0",
    "parcel": "^2.2.0",
    "postcss": "^8.1.0",
    "postcss-preset-env": "^7.2.3"
  }
}
